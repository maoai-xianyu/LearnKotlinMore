package com.mao.cn.kotlinlearn.K07Collection.mutableCollection

/**
 * Created by zhangkun on 2017/6/7.
 *
 * Kotlin 集合框架：可变集合与不可变集合
 *
 * 所有 Collection 和 Map 类型都有 可变 与 不可变 两种类型；
 *
 * Iterator 接口添加了迭代 Int、Long、Char、Short、Float、Double 六种基本数据类型的抽象类；
 *
 * 添加了大量扩展函数，方便对集合进行操作。
 *
 * Iterable<E> 与 MutableIterable<E>；
 *
 * Iterator<E> 与 MutableIterator<E>；
 *
 * Collection<E> 与 MutableCollection<E>；
 *
 * List<E> 与 MutableList<E>；
 *
 * Set<E> 与 MutableSet<E>；
 *
 * ListIterator<E> 与 MutableListIterator<E>；
 *
 * Map<E> 与 MutableMap<E>；
 *
 *
 */
